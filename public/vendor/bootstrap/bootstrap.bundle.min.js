/*! Mini Bootstrap JS stub (fallback) */
(function(){
  // No-op: solo evita errores si el CDN no carga.
  // Si luego querés tooltips/modals reales, reemplazá este archivo
  // por el oficial bootstrap.bundle.min.js.
  window.bootstrap = window.bootstrap || {};
})();
